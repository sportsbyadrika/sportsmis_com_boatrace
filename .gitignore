# Environment / secrets
app/.env
.env

# Composer
/vendor/
composer.phar

# Uploads (user content) — keep the directories, not the files
app/public/assets/uploads/**/*
!app/public/assets/uploads/**/.gitkeep

# Scratch / build
/storage/dompdf/*
!/storage/dompdf/.gitkeep
*.log
.DS_Store
Thumbs.db
